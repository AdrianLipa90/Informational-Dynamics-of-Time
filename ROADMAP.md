# Roadmap

## Phase A — Temporal core

Status: `FORMAL_CLOSURE_PASS / PHYSICAL_ADMISSION_OPEN`

Completed formal targets:

1. typed temporal state and composable transport;
2. phase cocycle and scalar phase transport;
3. smooth transport separated from reduced atomic event support;
4. NOW candidate localized as atomic support;
5. exact injective pushforward-support theorem (`T023`);
6. bifurcation representation requirements and generator subclass (`T024`).

Remaining upstream debt: `A0-ORDER-RANK` — physical construction of the informational rank that can induce order through `T005`.

## Phase B — Temporal transport and memory

Status: `TRANSPORT OPEN / MEMORY GATED`

Current work:

- event-interrupted propagator `T030`;
- classification of reversible and information-changing bifurcation classes;
- transport invariants and composition across multiple events.

Memory opens after temporal transport has a stable operator contract and receipts.

## Phase C — Retrodiction and retrocausal tests

Open after memory operators have independent receipts.

## Phase D — Spatial branch and Einstein closure

Spatial structure enters after the temporal branch has an independent canonical state, transport law and clock-calibration bridge.
