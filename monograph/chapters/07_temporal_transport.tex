\chapter{Temporal Transport and Internal Elapsed Activity}

The positive activity layer makes it possible to define an internal elapsed coordinate before introducing metric clock time. Let $\lambda$ be an admissible increasing ordering parameter, let $\mathfrak a(\lambda)>0$ denote realized temporal activity, and let $\mathfrak a_\star>0$ be a declared system-internal reference activity. Define
\[
\boxed{
d\tau_{\rm int}
=\frac{\mathfrak a(\lambda)}{\mathfrak a_\star}\,d\lambda.
}
\]
For a discrete ordered path,
\[
\boxed{
\tau_{{\rm int},N}
=\sum_{n=0}^{N-1}
\frac{\mathfrak a_n}{\mathfrak a_\star}\,\Delta\lambda_n,
\qquad
\Delta\lambda_n>0.
}
\]
Every admitted increment is strictly positive, so the internal elapsed coordinate is strictly monotone along the realized ordered path.

\section{Reparameterization covariance}

For an increasing relabeling $\lambda'=f(\lambda)$, let activity transform as a one-density,
\[
\mathfrak a'(\lambda')
=\mathfrak a(\lambda)\frac{d\lambda}{d\lambda'}.
\]
Then
\[
\mathfrak a'(\lambda')\,d\lambda'
=\mathfrak a(\lambda)\,d\lambda,
\]
and hence
\[
\boxed{d\tau'_{\rm int}=d\tau_{\rm int}.}
\]
The construction therefore depends on realized activity rather than on an arbitrary choice of the ordering label.

\section{Relational density, viscosity and internal pace}

Using the kinetic activity
\[
\mathfrak a_{ab}
=2M_{ab}\cosh(A_{ab}/2),
\]
with
\[
M_{ab}
=\frac{\sqrt{\rho_R(a)\rho_R(b)}}{\tfrac12[\eta_R(a)+\eta_R(b)]},
\]
gives
\[
\boxed{
\frac{d\tau_{\rm int}}{d\lambda}
=\frac{2}{\mathfrak a_\star}
\frac{\sqrt{\rho_R(a)\rho_R(b)}}{\tfrac12[\eta_R(a)+\eta_R(b)]}
\cosh(A_{ab}/2).
}
\]
Within this candidate, relational density raises the internal activity pace and relational viscosity lowers it. Reversing the antisymmetric drive leaves the pace unchanged because $\cosh$ is even, while the directed current changes sign through $\sinh$.

\begin{figure}[H]
\centering
\includegraphics[width=0.86\textwidth]{figures/internal_elapsed_activity.png}
\caption{Two systems traversing the same ordering interval can accumulate different internal elapsed activity when their relational activity differs. The plot is generated by \texttt{scripts/build\_internal\_elapsed\_figure.py}.}
\label{fig:internal-elapsed-activity}
\end{figure}

This quantity is an internal dynamical clock candidate. Its identification with experienced subjective time and its calibration to metric time remain later evidence and closure problems.
