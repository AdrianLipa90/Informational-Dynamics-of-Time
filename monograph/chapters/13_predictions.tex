\chapter{Predictions}

\noindent\textbf{Status:} \texttt{PREDICTION REGISTRY / MODEL-INTERNAL AND PHYSICAL TEST TARGETS SEPARATED}.

The temporal programme produces predictions at several evidential levels. Some are exact consequences of admitted definitions and reference dynamics; some are conditional physical relations whose calibration variables are explicit; others are preregistered targets awaiting data. Keeping these classes separate makes each prediction falsifiable at the layer where it is generated.

\section{P1: positive relational clock ratio}

For any two admitted clocks on the same ordered patch,
\[
\boxed{
N_R(x|r)=\frac{\mathfrak a_x}{\mathfrak a_r}>0.
}
\]
The prediction is invariant under increasing reparameterisation of the common order variable and composes as
\[
N_{x|s}=N_{x|r}N_{r|s}.
\]
After reference calibration,
\[
d\widehat\tau_x=N_R(x|r)dt.
\]
A physical clock experiment can therefore compare an independently estimated activity ratio with an observed elapsed-time ratio. The decisive residual is
\[
\Delta_N
=
\frac{d\widehat\tau_x}{dt}
-
\frac{\mathfrak a_x}{\mathfrak a_r}.
\]

\section{P2: relative-information clock potential}

For the admitted local memoryless positive-rate realisation, the clock comparison generates the exact KL potential
\[
\boxed{
\Phi(N_R)=N_R-1-\ln N_R.
}
\]
Its reference point is the unique minimum and its local Hessian is
\[
\Phi''(1)=1.
\]
The near-reference expansion begins quadratically,
\[
\Phi(1+\epsilon)=\frac12\epsilon^2-rac13\epsilon^3+O(\epsilon^4).
\]
This supplies a direct information-geometric prediction for any experiment realising the stated exponential holding-time class. The weak-field bridge additionally tests
\[
\Phi_N=c^2\ln N_R
\]
against independently measured gravitational clock potentials.

\section{P3: temporal correlations and spectral lines}

Once the internal elapsed coordinate is bound to a physical clock, a stationary discrete state system predicts
\[
C_{\mu\mu}(\Delta t)
=
\sum_{n,m}p_n|\mu_{mn}|^2
\exp\left[-\frac{i(E_m-E_n)\Delta t}{\hbar}\right].
\]
Consequently,
\[
\boxed{
\omega_{mn}=\frac{E_m-E_n}{\hbar}
}
\]
and, in the infinite-coherence limit,
\[
S(\omega)
\propto
\sum_{n,m}p_n|\mu_{mn}|^2
\delta(\omega-\omega_{mn}).
\]
Finite coherence broadens the supports into lines of finite width. The line set is therefore a frequency-domain image of discrete relative temporal phases.

The half-interface kernel
\[
\mathcal D_{1/2}(p,\Delta\tau)
=1+2\sqrt{p(1-p)}\cos(\Delta\tau/2)
\]
has the exact null
\[
p=\frac12,
\qquad
\Delta\tau\equiv2\pi\pmod{4\pi}.
\]
Where a physical two-channel system realises the required equal-weight and phase conditions, this predicts a dark/suppressed channel at the null.

\section{P4: exact retrodiction in the admitted memory reference class}

For the stratified positive-radius domain, the ordered active-attractor sequence \(\alpha\), winding packet \(\mathcal W\), pre-final radial packet, and final retained position construct the full position lineage:
\[
(\alpha,\mathcal W,\rho_1,\ldots,\rho_{N-1},r_N)
\xrightarrow{L_s^{\rho W}}
(r_1,\ldots,r_N).
\]
The 07K inverse then reconstructs the ordered event-kick lineage:
\[
(r_1,\ldots,r_N)
\longrightarrow
(u_1,\ldots,u_N).
\]
The prediction is exact on the declared decoder domain. It also predicts the scalar-carrier reduction
\[
\boxed{
N_{\rm radial}=N-1
=rac12(2N-2)
=rac12N_{\rm Cartesian}.
}
\]
A residence-bound radial commitment chain must reproduce the same packet and reject tampering, source-cell mismatch, post-state mismatch, active-attractor mismatch, and broken checkpoint order.

The broader global-domain programme predicts that any further ambiguity must appear as an explicit collision or singular stratum in the retained quotient/fibre coordinates. This turns global injectivity from a qualitative expectation into a finite set of collision searches and constructive lift tests.

\section{P5: chronology-sensitive withheld-lineage reconstruction}

E003 predicts a sharp separation between the true ordered checkpoint lineage and chronology-destroyed controls. For the admitted reference class, a full-rank sensitivity matrix should permit near-machine-precision recovery of the hidden kick coordinate, whereas shuffled checkpoint blocks should produce a much larger weighted residual
\[
Q=r^T\Sigma_Y^{-1}r.
\]
The existing reference runs satisfy this pattern. Future model families can challenge it by increasing kick count, changing checkpoint sparsity, and approaching singular active-attractor transitions.

\section{P6: preregistered future-conditioned physical test}

The prospective E004 experiment targets
\[
\boxed{
P(X_{\mathrm{past}}\mid Y_{\mathrm{future}})
\stackrel{?}{\neq}
P(X_{\mathrm{past}}).
}
\]
The test is meaningful only together with the preregistered chronology of event creation, condition assignment, commitment, unblinding, and analysis. A positive candidate requires survival of sham conditions, classical-channel audit, scheduler/shared-clock controls, fixed selection rules, and independent replication.

This is the point at which the theory makes its strongest temporal-order discrimination. Retrodiction predicts recovery of hidden past variables from later evidence while preserving the past record. The E004 candidate asks whether later condition selection changes the distribution of an already committed earlier observable under controls that exclude ordinary forward channels.

\section{P7: spacetime phase closure}

After independent temporal and spatial development, recombination predicts consistency with
\[
\boxed{
\Delta\phi
=
\frac{1}{\hbar}
\left(\mathbf p\cdot\Delta\mathbf x-E\Delta t\right).
}
\]
The temporal contribution is supplied through the calibrated clock/phase bridge and the spatial contribution through the external relational-geometric branch. This is a direct closure test because either branch or their interface can be falsified by a mismatch in the combined phase.

\section{P8: Einstein clock-lapse consistency}

The temporal branch and geometric branch independently generate a local/reference clock ratio. Their closure prediction is
\[
\boxed{
N_R^{\rm temporal}(x|r)
=
N_R^{\rm geometric}(x|r)
}
\]
within the domain of the admitted Einstein source solution and clock calibration. In the weak regime this can be expressed through
\[
\Phi_N=c^2\ln N_R.
\]
A multi-clock experiment with independently characterised source geometry is therefore a direct route to testing the temporal-to-Einstein bridge.

\section{P9: directional stress and spin-2 response}

For a conserved ultrarelativistic directional source,
\[
T^{\mu\nu}_\nu=\sum_a u_a k_a^\mu k_a^\nu,
\]
the TT components are
\[
T_+=\frac12(T_{xx}-T_{yy}),
\qquad
T_\times=T_{xy}.
\]
Their spin-2 phase coordinate is
\[
\phi_{\rm obs}
=
\frac12\operatorname{atan2}(T_\times,T_+)\pmod\pi,
\]
and the linearised far-zone response is
\[
h_{ij}^{TT}=rac{4G}{c^4r}\mathcal T_{ij}^{TT}.
\]
The IceTracks adapter turns this into a real-data programme: reconstruct the angular second moment, apply the declared detector-response calibration, estimate TT stress and its uncertainty, and compare the phase/amplitude channels to the preregistered source model. Isotropic directional ensembles provide a built-in TT null.

\section{P10: Euclidean horizon winding and spin structure}

For a non-extremal Euclidean horizon,
\[
\beta_H=\frac{2\pi}{\kappa_H},
\qquad
\oint\kappa_Hd\tau_E=2\pi.
\]
The thermal prediction is
\[
\boxed{
T_H=\frac{\hbar\kappa_H}{2\pi c k_B}.
}
\]
The same primitive frame winding predicts
\[
W_m=e^{i2\pi m}=1
\]
for integer spin and
\[
\boxed{W_{1/2}=-1}
\]
for spin \(1/2\). The latter produces
\[
\omega_n^F
=\left(n+\frac12\right)\kappa_H,
\]
while bosonic periodic modes give \(\omega_n^B=n\kappa_H\). These are exact horizon-geometry boundary conditions for any later microscopic emission model built on the temporal phase architecture.

\section{Prediction discipline}

Every prediction in this chapter is paired with a dependency and an evidence class. Exact algebra is tested by reference identities and adversarial cases. Reference dynamics are tested by withheld variables and null models. Physical predictions require calibration, uncertainty propagation, immutable raw data, and an explicit promotion receipt. This structure allows the programme to accumulate positive and negative results without changing the meaning of earlier claims.
