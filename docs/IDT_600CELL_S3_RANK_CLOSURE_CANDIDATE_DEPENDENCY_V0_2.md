# IDT 600-cell / S3 Rank Closure Candidate Dependency v0.2

Status: `CANDIDATE / ACTIVE_WORKING_DEPENDENCY / CANONICAL_TIME_DYNAMICS_UNCHANGED`

Date: 2026-09-11

IDT admits the TIR/PNCS 600-cell rank-closure result as an active candidate dependency for finite state-support and transport experiments.

Candidate inputs admitted:

```text
ell=0..5 : full-dimensional S3 harmonic sampling
ell=6    : first sampled-rank collapse, 49 -> 25
ell=7    : cumulative sampled space closes from 116 -> 120, adding 4 new directions
```

Permitted candidate use:

- finite angular/state support experiments;
- comparison of continuous and finite transport sectors;
- candidate event/state sampling diagnostics;
- candidate coupling to PhaseNav spectral and soliton experiments.

Not promoted by this dependency:

- `ell=6` as a physical event horizon;
- `ell=7` as a literal higher fractal level;
- a change to the IDT event clock, occurrence semantics, terminal-state binding, holonomy, foliation, or causal ordering;
- physical compactification or atomic-orbital identity.

`CANDIDATE` means active working dependency. IDT candidate experiments may consume it provided receipts retain the candidate status and do not satisfy canonical IDT gates without separate promotion.
