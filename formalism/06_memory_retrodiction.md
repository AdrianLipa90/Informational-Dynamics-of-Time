# 06 — Memory and Retrodiction: Kepler–Newton Memory Dynamics

Status: `PROVISIONAL_DOWNSTREAM_BRANCH / REFERENCE_IMPLEMENTATION_PASS`

Dependency position:

\[
\mathrm{Temporal\ Transport}\rightarrow\mathrm{Memory}\rightarrow\mathrm{Retrodiction}.
\]

The current admitted frontier remains Temporal Transport. This file records the first executable memory-dynamics candidate downstream of that frontier.

## 1. Memory phase plane

Represent the memory coordinate by
\[
m(\tau_{\rm int})=x_M(\tau_{\rm int})+iy_M(\tau_{\rm int}),
\qquad
r_M=|m|.
\]
The internal evolution parameter is inherited from the temporal-activity construction,
\[
d\tau_{\rm int}=\frac{\mathfrak a}{\mathfrak a_\star}\,d\lambda.
\]
This keeps memory propagation downstream of the already-defined temporal activity clock.

## 2. Newton generator on the memory plane

Introduce the effective memory parameter \(\mu_M>0\) and the central potential
\[
V_M(r_M)=-\frac{\mu_M}{r_M}.
\]
The smooth memory trajectory is generated by
\[
\boxed{
\frac{d^2m}{d\tau_{\rm int}^2}
=-\mu_M\frac{m}{|m|^3}.
}
\]
This is claim T016. The derivation of \(\mu_M\) from upstream relational quantities is an open derivation target.

For implementation, a temporal step first computes
\[
\Delta\tau_{\rm int}
=\frac{\mathfrak a}{\mathfrak a_\star}\Delta\lambda,
\]
and then advances the Newton system in \(\tau_{\rm int}\). This avoids introducing a separate clock into the memory layer.

## 3. Kepler invariants and areal law

For a smooth central segment define the specific memory energy
\[
\varepsilon_M
=\frac12\left|\frac{dm}{d\tau_{\rm int}}\right|^2
-\frac{\mu_M}{r_M},
\]
and signed angular momentum
\[
h_M
=\operatorname{Im}\!\left[m^*\frac{dm}{d\tau_{\rm int}}\right].
\]
Central-force evolution gives
\[
\boxed{
\frac{d\mathcal A_M}{d\tau_{\rm int}}=\frac{h_M}{2}.
}
\]
This is T017: equal signed memory areas are swept in equal internal-time intervals whenever \(h_M\) is constant.

The conic orbit law is
\[
\boxed{
r_M(\nu)=\frac{p_M}{1+e_M\cos\nu},
\qquad
p_M=\frac{h_M^2}{\mu_M}.
}
\]
For a bound nondegenerate branch,
\[
a_M=-\frac{\mu_M}{2\varepsilon_M},
\qquad
\boxed{
T_M^2=\frac{4\pi^2}{\mu_M}a_M^3.
}
\]
These relations form T018.

## 4. Connection to the existing memory circulation

The earlier oriented memory-edge drive obeys
\[
\sum_C A_e^{(M)}=2\lambda_M\mathcal A_M(C).
\]
Combining it with the areal law yields, on a smooth Kepler segment,
\[
\boxed{
\frac{d}{d\tau_{\rm int}}
\left(\sum_C A_e^{(M)}\right)
=\lambda_M h_M.
}
\]
Thus the signed memory circulation has a constant internal-time rate whenever the central-memory angular momentum is conserved.

## 5. Event impulse interface

A localized bifurcation may update the memory velocity through the typed interface
\[
\boxed{
\mathbf v_M^+=\mathbf v_M^-+\Delta\mathbf v_M.
}
\]
The projection from an admitted event imprint into \(\Delta\mathbf v_M\) is an open derivation target. The executable layer therefore accepts the impulse explicitly and records the resulting change in \(\varepsilon_M\), \(h_M\), eccentricity and orbit class.

## 6. Numerical reference

`src/idt/kepler_memory.py` uses velocity-Verlet propagation for the smooth central-force segment. The reference implementation exposes:

- Newton inverse-square acceleration;
- specific energy and signed angular momentum;
- eccentricity vector, conic class, semi-major axis and Kepler period;
- signed areal velocity and accumulated swept area;
- temporal-activity-to-\(\Delta\tau_{\rm int}\) coupling;
- explicit event impulse updates.

The targeted validation artifact is `validation/KEPLER_MEMORY_DYNAMICS_V0_1.json`.
